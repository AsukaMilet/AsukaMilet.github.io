<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>栈和队列</title>
    <url>/2022/01/28/%E6%A0%88%E5%92%8C%E9%98%9F%E5%88%97/</url>
    <content><![CDATA[<h1 id="栈和队列">栈和队列</h1>
<h2 id="栈">1.栈</h2>
<p><strong>栈</strong>(stack)是一个带有限制的表，它的插入和删除只能在一个位置上进行，即只能在表的末端进行，表的末端称为<strong>栈顶</strong>(top)</p>
<hr />
<h3 id="栈模型">1.1栈模型</h3>
<p>对栈的基本操作就是push(进栈)和pop(出栈)，前者等价于一次插入，后者则是删除最新插入的元素。最近插入的元素在执行pop之前可以通过使用top操作进行使用。在栈ADT中，对空栈执行top或pop一般均被认为是一个错误。另一方面，执行push时空间用尽是一个实现限制，但不是ADT错误。</p>
<hr />
<p>栈有时又叫作<strong>LIFO(后进先出)表</strong>，普通的清空栈操作和判断是否空栈的测试都是栈的操作指令系统的一部分，但是对栈所能够做的基本就是push和pop操作。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-28%20203951.png" /></p>
<center>
<p>
栈模型:通过push输入，通过pop和top输出
</p>
</center>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-28%20205109.png" /></p>
<center>
<p>
栈模型：只有栈顶元素可访问
</p>
</center>
<hr />
<h3 id="栈的实现">1.2栈的实现</h3>
<h4 id="栈的stl实现">栈的STL实现</h4>
<p><a
href="https://www.cplusplus.com/reference/stack/stack/">STL中的Stack</a></p>
<h4 id="栈的数组实现">栈的数组实现</h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//C++ Version</span></span><br><span class="line"><span class="keyword">int</span> Stack[MAX];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Initialization</span><span class="params">(<span class="keyword">int</span> top)</span>		<span class="comment">//清空栈</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    top=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsEmpty</span><span class="params">(<span class="keyword">int</span> top)</span>				<span class="comment">//判断是否为空栈</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(top==<span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsFull</span><span class="params">(<span class="keyword">int</span> top)</span>				<span class="comment">//判断是否满栈</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(top==MAX<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Push</span><span class="params">(<span class="keyword">int</span> num,<span class="keyword">int</span>*Stack)</span>		<span class="comment">//入栈</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">IsFull</span>(top))</span><br><span class="line">        <span class="comment">//报错</span></span><br><span class="line">    top++;</span><br><span class="line">    Stack[top]=num;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Pop</span><span class="params">(<span class="keyword">int</span> top)</span>					<span class="comment">//弹栈</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">IsEmpty</span>(top))</span><br><span class="line">        <span class="comment">//报错</span></span><br><span class="line">    top--;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Top</span><span class="params">(<span class="keyword">int</span>*Stack)</span>					<span class="comment">//返回栈顶元素</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Stack[top];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h3 id="栈的应用">1.3栈的应用</h3>
<ul>
<li><p>平衡符号<strong>(联机算法)</strong>：做一个空栈。读入字符直到文件尾。如果字符是一个开放符号('['、'{'等)，则将其推入栈中。如果一个字符是一个封闭符号，则当栈空时报错。否则，将栈元素弹出。如果弹出的符号不是对应的开放符号，则报错。在文件尾，如果栈非空则报错。</p>
<p><a
href="https://www.luogu.com.cn/problem/P1739">洛谷P1739</a></p></li>
<li><p>后缀表达式</p>
<p><a
href="https://www.luogu.com.cn/problem/P1449">洛谷P1449</a></p></li>
<li><p>中缀到后缀的转换</p></li>
</ul>
<hr />
<h3 id="单调栈">1.4单调栈</h3>
<p>单调栈即满足单调性的一种特殊栈结构</p>
<h4 id="插入">插入</h4>
<p>将一个元素插入单调栈时，为了维护栈的单调性，需要在保证将该元素插入到栈顶后整个栈满足单调性的前提下弹出最少的元素。</p>
<p>实现如下:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//插入X</span></span><br><span class="line"><span class="keyword">while</span>(!sta.<span class="built_in">empty</span>()&amp;&amp;sta.<span class="built_in">top</span>()&lt;X)</span><br><span class="line">    sta.<span class="built_in">pop</span>();</span><br><span class="line">sta.<span class="built_in">push</span>(X);</span><br></pre></td></tr></table></figure>
<h4 id="使用">使用</h4>
<p>从栈顶读取元素，读取元素为栈中最大值或最小值</p>
<h4 id="应用">应用</h4>
<ul>
<li><p><a
href="https://www.luogu.com.cn/problem/P5788">洛谷P5788</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P6198">洛谷P6198</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P6510">洛谷P6510</a></p></li>
</ul>
<hr />
<h2 id="队列">2.队列</h2>
<p><strong>队列</strong>(queue)也是一种表。使用队列时插入在一端进行而删除则在另一端进行。</p>
<h3 id="队列模型">2.1队列模型</h3>
<p>队列的基本操作是enqueue(入队)，它是在表的末端(称为<strong>队尾</strong>(rear))插入一个元素，一个dequeue(出队)，它是删除(并返回)在表的开头(叫作<strong>队头</strong>(front))的元素。队列也被称为<strong>FIFO(先进先出)表</strong>。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-28%20215110.png" /></p>
<center>
<p>
队列模型
</p>
</center>
<hr />
<h3 id="队列的实现">2.2队列的实现</h3>
<h4 id="队列的stl实现">队列的STL实现</h4>
<p><a
href="https://www.cplusplus.com/reference/queue/queue/">STL中的队列</a></p>
<h4 id="队列的数组实现">队列的数组实现</h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">// C++ Version</span></span><br><span class="line"><span class="keyword">int</span> Queue[MAX];</span><br><span class="line"><span class="keyword">int</span> head,tail;						<span class="comment">//head表示队首，tail表示队尾</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Initialization</span><span class="params">(<span class="keyword">int</span> head,<span class="keyword">int</span> tail)</span>	<span class="comment">//清空队列</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    head=tail=<span class="number">0</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsEmpty</span><span class="params">(<span class="keyword">int</span> head,<span class="keyword">int</span> tail)</span>			<span class="comment">//判断是否为空队列</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(head==tail)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsFull</span><span class="params">(<span class="keyword">int</span> head,<span class="keyword">int</span> tail)</span>			<span class="comment">//判断队列是否已满</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(head==(tail+<span class="number">1</span>)%MAX)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Enqueue</span><span class="params">(<span class="keyword">int</span> num,<span class="keyword">int</span>*Queue)</span>			<span class="comment">//入队</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">IsFull</span>(head,tail))</span><br><span class="line">        <span class="comment">//报错</span></span><br><span class="line">    Queue[tail]=num;</span><br><span class="line">    <span class="keyword">if</span>(tail+<span class="number">1</span>==MAX)</span><br><span class="line">        tail=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        tail++;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Dequeue</span><span class="params">(<span class="keyword">int</span> num,<span class="keyword">int</span>*Queue)</span>			<span class="comment">//出队</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(<span class="built_in">IsEmpty</span>(<span class="keyword">int</span> head,<span class="keyword">int</span> tail))</span><br><span class="line">        <span class="comment">//报错</span></span><br><span class="line">    <span class="keyword">if</span>(head+<span class="number">1</span>==MAX)</span><br><span class="line">        head=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        head++;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Front</span><span class="params">(<span class="keyword">int</span> head,<span class="keyword">int</span>*Queue)</span>			<span class="comment">//取得队首元素</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">return</span> Queue[head];</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//若要实现双端队列操作，只需传入tail即可</span></span><br></pre></td></tr></table></figure>
<hr />
<h3 id="队列的应用">2.3队列的应用</h3>
<ul>
<li><p><a
href="https://www.acwing.com/problem/content/134/">AcwingP132</a></p></li>
<li><p><a
href="https://www.acwing.com/problem/content/831/">AcwingP829</a></p></li>
<li><p><a
href="https://www.acwing.com/problem/content/3551/">AcwingP3548</a></p></li>
</ul>
<h3 id="单调队列">2.4单调队列</h3>
<p>单调队列中的元素应满足单调性<strong>递增</strong>或<strong>递减</strong>，且元素只能从队头与队尾进行操作。</p>
<h4 id="插入-1">插入</h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//插入X</span></span><br><span class="line"><span class="keyword">while</span>(!que.<span class="built_in">empty</span>()&amp;&amp;que.<span class="built_in">back</span>()&gt;X)			<span class="comment">//若队尾元素比待插入值大，循环弹出</span></span><br><span class="line">    que.<span class="built_in">pop_back</span>();</span><br><span class="line">que.<span class="built_in">push_back</span>(X);							</span><br></pre></td></tr></table></figure>
<h4 id="使用-1">使用</h4>
<p>队首元素为最大值或最小值</p>
<ul>
<li><p><a
href="https://www.acwing.com/problem/content/156/">AcwingP154</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P2032">洛谷P2032</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P1638">洛谷P1638</a></p></li>
</ul>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: 2022-1-28</p>
]]></content>
      <categories>
        <category>Data Structrue</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>线性表</tag>
      </tags>
  </entry>
  <entry>
    <title>不相交集（DisjSets）的原理及实现</title>
    <url>/2022/01/26/%E4%B8%8D%E7%9B%B8%E4%BA%A4%E9%9B%86%EF%BC%88DisjSets%EF%BC%89%E7%9A%84%E5%8E%9F%E7%90%86%E5%8F%8A%E5%AE%9E%E7%8E%B0/</url>
    <content><![CDATA[<h1
id="不相交集disjsets的原理及实现">不相交集（DisjSets）的原理及实现</h1>
<h2 id="不相交集基本知识">1.不相交集基本知识</h2>
<p>不相交集主要涉及两种操作：合并和查找。初始时，不同的集合中元素不相交(<span
class="math inline">\(S_i \cap
S_j=\varnothing\)</span>),经过一系列Union操作后,合并为一个大的集合。</p>
<p>DisjSets是一种动态集合（随着Union操作的增多，动态集合中集合个数减少）
<span class="math display">\[
S=\{s_1,s_2,s_3....s_n\}
\]</span> 判断某个元素是否在集合<span
class="math inline">\(s_i\)</span>中,只需进行Find操作，返回值为包含给定元素集合的名字。判断两个元素是否在同一个集合中只需要比较Find(X)和Find(Y)是否返回同一个集合名字即可。</p>
<p>不相交集的应用范围：</p>
<ol type="1">
<li><strong><em>判断无向图的连通分量个数</em></strong></li>
<li><strong><em>判断无向图中任意两个顶点是否连通</em></strong></li>
<li>......</li>
</ol>
<h2 id="不相交集的基本实现c">2.不相交集的基本实现（C++）</h2>
<h3 id="class实现">（1）Class实现</h3>
<h4 id="adt接口实现一维数组实现">①ADT接口实现（一维数组实现）</h4>
<p>不相交集<strong>S</strong>由若干子集合<strong><span
class="math inline">\(s_i\)</span></strong>构成，不相交集的逻辑结构就是一个森林（树的集合）。<strong><span
class="math inline">\(s_i\)</span></strong>表示森林中的一颗子树，一般以该子树的根作为子树代表。</p>
<p>由于需要的唯一信息为<strong>父链（parent
link）</strong>，集合的名字由根处的节点给出，由于只需要知道父节点的名字，可以假设这棵树被非显式存储在一个数组中：数组的每个成员<strong>s[i]</strong>表示元素<strong>i</strong>的父亲，如果<strong>i</strong>为根，则<strong>s[i]=i</strong>（<em>e.g.</em>
s[5]=4，表示节点5的父亲为节点4）</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">DisjSets</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">DisjSets</span><span class="params">(<span class="keyword">int</span> ElementsNum)</span></span>;   <span class="comment">//构造函数，初始化DisjSets</span></span><br><span class="line">    <span class="function"><span class="keyword">int</span> <span class="title">Find</span><span class="params">(<span class="keyword">int</span> num)</span></span>;                    <span class="comment">//Find操作</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">UnionSets</span><span class="params">(<span class="keyword">int</span> num1,<span class="keyword">int</span> num2)</span></span>;    <span class="comment">//Union操作</span></span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    vector&lt;<span class="keyword">int</span>&gt; s;</span><br><span class="line">    vector&lt;<span class="keyword">int</span>&gt; rank;					  <span class="comment">//保存每个子树的秩</span></span><br><span class="line">    <span class="keyword">int</span> count;                            <span class="comment">//记录DisjSets中子集合的个数（子树个数）</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>初始化操作如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">DisjSets::<span class="built_in">DisjSets</span>(<span class="keyword">int</span> ElementsNum)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;ElementsNum;i++)      <span class="comment">//若编号从1开始，则ElementsNum应为输入元素个数加1</span></span><br><span class="line">    &#123;</span><br><span class="line">        s.<span class="built_in">push_back</span>(i);</span><br><span class="line">        rank.<span class="built_in">push_back</span>(<span class="number">0</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    count=ElementsNum;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="基本操作实现">②基本操作实现</h4>
<p>Union操作即将两个不相交的子集和合并成一个大集合,只需将一颗子树的父节点指向另一颗子树即可</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DisjSets::UnionSets</span><span class="params">(<span class="keyword">int</span> num1,<span class="keyword">int</span> num2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> root1=<span class="built_in">Find</span>(num1);          <span class="comment">//查找num1元素的祖先</span></span><br><span class="line">    <span class="keyword">int</span> root2=<span class="built_in">Find</span>(num2);          <span class="comment">//查找num2元素的祖先</span></span><br><span class="line">    s[root2]=root1；               <span class="comment">//将num2祖先变为num1祖先的儿子</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Find操作即查找所需元素所在集合，即返回所在元素名字。</p>
<p><strong><em>e.g</em></strong> 并查集如下：</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/715283-20160917221621664-276304966.png" /></p>
<p>此时共有四个子树，第一个子树名字为0，第二个子树名字为1，第三个子树名字为3，第四个子树名字为5。所以Find(2)返回值为1，Find(4)返回值为3，Find(3)返回值为3。Find基本实现如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">DisjSets::Find</span><span class="params">(<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(s[num]==num)</span><br><span class="line">        <span class="keyword">return</span> num;                </span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">Find</span>(s[x]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="unionfind操作的改进">③Union/Find操作的改进</h4>
<h5 id="i.按高度秩rank求并">I.按高度（秩（rank））求并</h5>
<p>上述Union操作很随意：任选一颗子树，将另一颗子树的根指向它就完成合并。若一直按上述操作合并，可能产生非常不平衡的子树，如：</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-26%20195423.png" /></p>
<p><strong>树变得越来越高，此时会影响到Find的效率</strong>。</p>
<p>因此引入按秩合并（启发式合并）：将秩小的子树的根指向秩大的子树的根。</p>
<p>秩的定义：对每个节点，用秩表示节点高度的上界（路径压缩不完全与秩兼容，路径压缩会改变树的高度，此时不追求树的高度的精确值，而是存储树的高度的估计值，树的高度的估计值为秩）</p>
<p>若不考虑路径压缩，则为<strong>按高度求并（union by
height）</strong>：在合并前，判断哪颗子树更高，让更矮的子树的根节点指向高的子树的根。</p>
<p>实现如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DisjSets::UnionSets</span><span class="params">(<span class="keyword">int</span> num1,<span class="keyword">int</span> num2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> root1=<span class="built_in">Find</span>(num1);</span><br><span class="line">    <span class="keyword">int</span> root2=<span class="built_in">Find</span>(num2);</span><br><span class="line">    <span class="keyword">if</span>(root1!=root2)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(rank[root1]&gt;rank[root2])                 <span class="comment">//num1所在子树更深</span></span><br><span class="line">        s[root2]=root1;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span>(rank[root1]&lt;rank[root2])            <span class="comment">//num2所在子树更深</span></span><br><span class="line">        s[root1]=root2;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            s[root2]=root1;                         <span class="comment">//将num2所在子树合并至num1所在子树</span></span><br><span class="line">            rank[root1]++;                          <span class="comment">//num1所在子树秩加深</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5
id="ii.路径压缩核心思路将查找路径上节点直接连接至根上">II.路径压缩（核心思路：将查找路径上节点直接连接至根上）</h5>
<p>对于上述Find操作，对于连续M个指令（在所有模型下）平均为线性的。但<strong><em>O（M<span
class="math inline">\(\log{N}\)</span>）</em></strong>的最坏情况仍有可能发生。例如，把所有集合放于一个队列中，并反复让前两个集合出队而使它们的并入队，则最坏情况就会发生。路径压缩操作相对于基础Find操作变化不大，使s[<strong><em>num</em></strong>]等于由Find返回的值。这样，在集合的根被递归地查找到后，<strong><em>num</em></strong>的父节点就设置为它，这对通向根的路径上的每一个节点递归地出现，因而实现路径压缩。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">DisjSets::Find</span><span class="params">(<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(s[num]==num)</span><br><span class="line">        <span class="keyword">return</span> num;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> s[num]=<span class="built_in">Find</span>(s[num]);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5
id="iii.路径压缩与按高度求并兼容问题">III.路径压缩与按高度求并兼容问题</h5>
<p>因为路径压缩可以改变树的高度，此时不清楚如何去重新计算树的高度，解决方案则是<strong>不计算树的高度</strong>，此时，对于每棵树所存储高度变为估计值（秩）。</p>
<p>若不使用按高度合并，只使用路径压缩最坏时间复杂度为<strong><em>O(m<span
class="math inline">\(\log{N}\)</span>)</em></strong>。</p>
<p>若只使用按高度求并，不使用路径压缩，时间复杂度为<strong><em>O(m<span
class="math inline">\(\log{n}\)</span>)</em></strong>，由于由于路径压缩单次合并可能造成大量修改，有时路径压缩并不适合使用。例如，在可持久化不相交集、线段树分治
+ 不相交集中，一般使用只按高度合并的不相交集。</p>
<p>时间复杂度与空间复杂度分析:</p>
<p><a href="https://oi-wiki.org/ds/dsu/">OI Wiki并查集</a></p>
<hr />
<h3 id="不相交集的数组实现">（2）不相交集的数组实现</h3>
<p>代码如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> DijsSets[MAX];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Initialization</span><span class="params">(<span class="keyword">int</span> size)</span>                 <span class="comment">//初始化DisjSets</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;size;i++)					  <span class="comment">//编号从0开始</span></span><br><span class="line">        DijsSets[i]=i;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">Find</span><span class="params">(<span class="keyword">int</span> num,<span class="keyword">int</span>*DisjSets)</span>                <span class="comment">//路径压缩查找</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(DisjSets[num]!=num)					  <span class="comment">//num不是该集合的根</span></span><br><span class="line">        DisjSets[num]=<span class="built_in">Find</span>(DisjSets[num],DisjSets);    <span class="comment">//递归搜索</span></span><br><span class="line">    <span class="keyword">return</span> num;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">UnionSets</span><span class="params">(<span class="keyword">int</span> num1,<span class="keyword">int</span> num2,<span class="keyword">int</span>*DisjSets)</span>         <span class="comment">//简单合并</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> root1=<span class="built_in">Find</span>(num1,DisjSets);</span><br><span class="line">    <span class="keyword">int</span> root2=<span class="built_in">Find</span>(num2,DisjSets);</span><br><span class="line">    DisjSets[root2]=root1;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="不相交集应用">3.不相交集应用</h2>
<p>应用本质：初始时为一个个不连通的对象，经过一步步处理，变成连通的。</p>
<ol type="1">
<li><p><strong>生成迷宫</strong>：<img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-26%20195521.png" /></p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-26%20195539.png" /></p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-01-26%20195615.png" /></p></li>
<li><p><strong>无向图的连通分量个数</strong>：初始时，将无向图中各个顶点视为不连通的子集合，对图中每一条边，相当于union这条边对应的两个顶点分别所在的集合，直至所有的边都处理完后，还剩下的集合的个数即为连通分量的个数。</p></li>
</ol>
<hr />
<h2 id="相关题目">相关题目</h2>
<ul>
<li><a href="https://www.luogu.com.cn/problem/P3367">洛谷P3367</a></li>
<li><a href="https://www.luogu.com.cn/problem/P1551">洛谷P1551</a></li>
<li><a href="https://www.luogu.com.cn/problem/P1621">洛谷P1621</a></li>
<li><a href="https://www.luogu.com.cn/problem/P2024">洛谷P2024</a></li>
<li><a href="https://www.luogu.com.cn/problem/P1196">洛谷P1196</a></li>
<li><a
href="https://www.acwing.com/problem/content/147/">AcwingP145</a></li>
</ul>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: 2022-1-26</p>
]]></content>
      <categories>
        <category>Data Structrue</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>不相交集</tag>
      </tags>
  </entry>
  <entry>
    <title>图论基础</title>
    <url>/2022/01/27/%E5%9B%BE%E8%AE%BA%E5%9F%BA%E7%A1%80/</url>
    <content><![CDATA[<h1 id="图论基础">图论基础</h1>
<h2 id="图的定义">1.图的定义</h2>
<ul>
<li>图是用来对对象之间的成对关系建模的数学结构，一个<strong>图(Graph)</strong>
<em>G=(V,E)</em>由"节点"或"顶点"(Vertex）以及连接这些顶点的"边"（Edge）组成。
<ul>
<li>对于一张图<strong><em>G=(V,E)</em></strong>,若存在另一张图<strong><em>H=(V',E')</em></strong>满足<strong><em>V'<span
class="math inline">\(\subseteq\)</span>V</em></strong>，且<strong><em>E'<span
class="math inline">\(\subseteq\)</span>E</em></strong>,则称<strong><em>H</em></strong>为<strong><em>G</em></strong>的<strong>子图(Subgraph)</strong>,记作<strong><em>H<span
class="math inline">\(\subseteq\)</span>G</em></strong>。</li>
</ul></li>
<li>每一条边就是一个点对<strong><em>(v,w)</em></strong>，其中<strong><em>v,w<span
class="math inline">\(\in\)</span>V</em></strong>。如果点对是有序的，那么图就叫作<strong>有向(directed)</strong>的。有向的图也被称为<strong>有向图(digraph)</strong>。图中的边在连接顶点时无需区分方向，则图为<strong>无向</strong>的，也称为<strong>无向图</strong>。
<ul>
<li>对于有向图<strong><em>G=(V,E)</em></strong>,它的<strong>反图(Transpose
Graph)</strong>指的是点集不变，每条边反向得到的图，即：若<strong><em>G</em></strong>的反图为<strong><em>G<span
class="math inline">\(^T\)</span>=(V,E')</em></strong>,则<strong><em>E'=</em></strong>
<strong>{(w,v)|(v,w)<span class="math inline">\(\in\)</span><em>E</em>
}</strong></li>
</ul></li>
<li>有时候边具有第三种成分，即<strong>权</strong>(weight)或<strong>值</strong>(cost)。边的<strong>权值</strong>记为<strong><em>w(v,w)</em></strong>。</li>
<li>顶点集合为<strong><em>V</em></strong>，边集合为<strong><em>E</em></strong>的图记为<strong><em>G=（V，E）</em></strong>，其顶点数为<strong><span
class="math inline">\(|V|\)</span></strong>,边数为<strong><span
class="math inline">\(|E|\)</span></strong>。</li>
<li>相邻顶点构成的顶点序列称为<strong>路径</strong>(path)，即一个顶点序列<span
class="math inline">\(w_1,w_2,w_3,...w_n\)</span>, 使(<span
class="math inline">\(w_i,w_{i+1}\)</span>)<span
class="math inline">\(\in\)</span><strong><em>E</em></strong>,且1<em><span
class="math inline">\(\leq\)</span></em> <em>i</em> <em><span
class="math inline">\(&lt;\)</span></em>
<em>N</em>。则一条路径的长为该路径上的边数，它等于<em>N-1</em>。起点若与终点相同，则称为<strong>环</strong>。</li>
<li>与顶点相连的边数称为<strong>度(Degree)</strong>，有向图的度又分为<strong>入度</strong>(以该顶点为终点)和<strong>出度</strong>(以该顶点为起点)。</li>
<li><strong>连通图</strong>：任意两个顶点都是连通的，即都可从第一个顶点到第二个顶点，也可以从第二个顶点到第一个顶点。
<ul>
<li>连通分量：无向图<strong><em>G</em></strong>的极大连通子图称为<strong><em>G</em></strong>的连通分量。极大连通子图：该子图为<strong><em>G</em></strong>的连通子图，如果再加入一个顶点，该子图不连通。</li>
</ul></li>
<li><strong>完全图</strong>：任意两个顶点间都有一条边，无向完全图共有
<strong><em>n<span
class="math inline">\(\times\)</span>(n-1)/2</em></strong>条边,有向完全图共有
<strong><em>n<span
class="math inline">\(\times\)</span>(n-1)</em></strong>条边。</li>
<li><strong>稀疏图</strong>：边数远小于完全图的图。
<strong>稠密图</strong>：边数接近于完全图的图</li>
</ul>
<hr />
<h2 id="图的存储表示c">2.图的存储（表示）(C++)</h2>
<h3 id="邻接矩阵">2.1邻接矩阵</h3>
<p>若有<strong><em>n</em></strong>个点，则开一个<strong><em>n<span
class="math inline">\(\times\)</span>​n</em></strong>的二维数组，其中一维表示起点，另一维表示终点，其存储的值可用于表示边的存在与否或者边的权值大小。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">int</span> Edge[MAX][MAX]					<span class="comment">//按边数表示</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Insert</span><span class="params">(<span class="keyword">int</span> v,<span class="keyword">int</span> w,<span class="keyword">int</span> value)</span>  <span class="comment">//将两个点建立关系</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Edge[v][w]=value;			    <span class="comment">//表示有向图</span></span><br><span class="line">    <span class="comment">//无向图表示为Edge[v][w]=Edge[w][v]=value</span></span><br><span class="line">    <span class="comment">//若无权值，可用1表示有边，0表示无边</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>邻接矩阵的空间复杂度为<strong><em>O(N<span
class="math inline">\(^2\)</span>)</em></strong>，若图中点较多，则大概率超出内存限制。</p>
<h4 id="复杂度">复杂度</h4>
<ul>
<li>查询是否存在某条边<strong><em>O(1)</em></strong></li>
<li>遍历连接一个点的所有边<strong><em>O(N)</em></strong></li>
<li>遍历整张图<strong><em>O(N<span
class="math inline">\(^2\)</span>)</em></strong></li>
</ul>
<h4 id="应用">应用</h4>
<p>邻接矩阵只适用于没有重边(或重边可以忽略)的情况。其最显著的优点是可以查询一条边是否存在。</p>
<p>由于邻接矩阵在稀疏图上效率很低(尤其是在点数较多的图上，空间无法承受)，所以一般只会在稠密图</p>
<p>上使用邻接矩阵。</p>
<hr />
<h3 id="邻接表">2.2邻接表</h3>
<p>若有<strong><em>n</em></strong>个点，则开一个大小为<strong><em>n</em></strong>的vector数组，其中数组下标表示起点，vector中存储的值为终点</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="keyword">int</span>&gt;Edge[MAX];						 <span class="comment">//存储vector&lt;int&gt;的数组</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Insert</span><span class="params">(<span class="keyword">int</span> v,<span class="keyword">int</span> w)</span>					 <span class="comment">//无权值情况</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Edge[v].<span class="built_in">push_back</span>(w);                    <span class="comment">//从v出发，到达终点为w</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>若边具有权值，则：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> end;							      <span class="comment">//存储中终点</span></span><br><span class="line">    <span class="keyword">int</span> value;</span><br><span class="line">&#125;;</span><br><span class="line">vector&lt;Node&gt;Edge[MAX];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Insert</span><span class="params">(<span class="keyword">int</span> v,<span class="keyword">int</span> w,<span class="keyword">int</span> value)</span>			  <span class="comment">//有权值情况</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Node temp;</span><br><span class="line">    temp.end=w;</span><br><span class="line">    temp.value=value;</span><br><span class="line">    Edge[v].<span class="built_in">push_back</span>(temp);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="复杂度-1">复杂度</h4>
<ul>
<li>查询是否存在<strong><em>u</em></strong>到<strong><em>v</em></strong>的边<strong><em>O(d(u))</em></strong></li>
<li>遍历点<strong><em>u</em></strong>的所有出边<strong><em>O(d(u))</em></strong></li>
<li>遍历整张图<strong><em>O(M+N)</em></strong></li>
<li>空间复杂度<strong><em>O(M)</em></strong></li>
</ul>
<p><strong>P.S </strong>
<strong><em>d(u)</em></strong>为以<strong><em>u</em></strong>为出发点的所有边数</p>
<h4 id="应用-1">应用</h4>
<p>存各种图都很适合，除非有特殊需求（如需要快速查询一条边是否存在，且点数较少，可以使用邻接矩阵）。尤其适用于需要对一个点的所有出边进行排序的场合。</p>
<hr />
<h4 id="链式前向星">2.3链式前向星</h4>
<p>本质是静态的链表式存储</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/1184092-20180710221842009-1426915822.png" /></p>
<p>基本结构如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> end;								<span class="comment">//终点</span></span><br><span class="line">    <span class="keyword">int</span> value;								<span class="comment">//边的权值</span></span><br><span class="line">    <span class="keyword">int</span> next;								</span><br><span class="line">&#125;;</span><br><span class="line">Node Edge[MAX];</span><br></pre></td></tr></table></figure>
<ol type="1">
<li><strong>对于next</strong>:</li>
</ol>
<p>举个例子，从节点1出发，节点编号最大的是节点5，节点5的next将会是3，而节点3的next将会是结点2，而到了节点2之后节点1的所有出边都已经被处理了，所有节点2的next将会是-1，这样子将可以找到对于节点1的所有直接相接的节点（注意：遍历时节点是由大到小遍历的，即是逆序的）。当检测到next的值为-1时会退出当前遍历。</p>
<p>如果节点的出度为0或者小于1，则对应的next值为-1。</p>
<ol start="2" type="1">
<li><strong>对于end</strong></li>
</ol>
<p>简单概括来说就是对于第<strong>i</strong>条边的终点，比如说输入第一组边（1，
2）的时候，Edge[0].end = 2（数组从0开始存储）。</p>
<p>如果是有向图，那么只要处理一次就够了。对于一个无向图的话，就需要来回存一次。</p>
<ol start="3" type="1">
<li><strong>对于head数组</strong></li>
</ol>
<p>对于输入的边，用来存以<strong>i</strong>为起点的边的存储位置。如果以<strong>i</strong>为起点的边出度大于1，此时head[i]会存入在输入中最后输入的以<strong>i</strong>为起点的编号。</p>
<p><strong><em>e.g</em></strong>
对于节点1，它的出度为3，由先往后分别输入了（1， 2）（1， 3）（1，
5），最后存入head的就会是5（2和3其实也有被存储的时候，但是后来会被5覆盖掉），即结果就是head[1]
= 5。</p>
<p>建立边方式如下：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//head[u]和k的初始值均为-1</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Insert</span><span class="params">(<span class="keyword">int</span> v,<span class="keyword">int</span> w,<span class="keyword">int</span> value)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    Edge[k].end=w;</span><br><span class="line">    Edge[k].value=value;</span><br><span class="line">    Edge[k].next=head[v];</span><br><span class="line">    head[v]=k++;					<span class="comment">//存储v这个值在第几组输入的边内出现过</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="复杂度-2">复杂度</h4>
<ul>
<li>查询是否存在<strong><em>u</em></strong>到<strong><em>v</em></strong>的边<strong><em>O(d(u))</em></strong></li>
<li>遍历点<strong><em>u</em></strong>的所有出边<strong><em>O(d(u))</em></strong></li>
<li>遍历整张图<strong><em>O(M+N)</em></strong></li>
<li>空间复杂度<strong><em>O(M)</em></strong></li>
</ul>
<p><strong>P.S </strong>
<strong><em>d(u)</em></strong>为以<strong><em>u</em></strong>为出发点的所有边数</p>
<h4 id="应用-2">应用</h4>
<p>存各种图都很适合，但不能快速查询一条边是否存在，也不能方便地对一个点的出边进行排序。</p>
<hr />
<h2 id="图的遍历">3.图的遍历</h2>
<p>图的遍历与搜索一样，主要分为<strong>DFS(深度优先搜索)</strong>与<strong>BFS(广度优先搜索)</strong></p>
<h3 id="dfs图论">3.1DFS(图论)</h3>
<p><strong>DFS的特点是，顺着当前的路径一直往深处遍历，直到无点可访问时，再返回上一级节点。</strong>DFS
会对其访问过的点打上访问标记，在遍历图时跳过已打过标记的点，以确保
<strong>每个点仅访问一次</strong>。符合以上两条规则的函数，便是广义上的
DFS。</p>
<p><strong><em>e.g</em></strong> <a
href="https://blog.csdn.net/saltriver/article/details/54429068">DFS(图论)图解</a></p>
<p>以邻接表为例:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//假设图的边有权值</span></span><br><span class="line">vector&lt;Node&gt;Edge[MAX];                               <span class="comment">//邻接表</span></span><br><span class="line"><span class="keyword">bool</span> visited[MAX];					<span class="comment">//存储节点是否被访问过</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Dfs</span><span class="params">(<span class="keyword">int</span> start)</span>					<span class="comment">//start为开始访问节点</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    visited[start]=<span class="literal">true</span>;			<span class="comment">//将开始节点设置为已访问</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[start].<span class="built_in">size</span>();i++)	      <span class="comment">//遍历当前节点可到达的所有终点</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> next=Edge[start][i].end;</span><br><span class="line">        <span class="keyword">if</span>(visited[next]==<span class="literal">false</span>)</span><br><span class="line">            <span class="built_in">Dfs</span>(next);</span><br><span class="line">        <span class="comment">//若next指向的节点尚未访问，则递归下去进行访问</span></span><br><span class="line">	&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="复杂度-3">复杂度</h4>
<p>时间复杂度通常为<strong><em>O(M+N)</em></strong>,空间复杂度为<strong><em>O(N)</em></strong>,N为图的点数，M为图的边数。</p>
<h3 id="bfs图论">3.2BFS（图论）</h3>
<p><strong>BFS的特点是，将当前所在节点可访问的所有节点都加入队列，等全都访问过后再前往下一层。</strong>BFS
算法找到的路径是从起点开始的<strong>最短</strong>合法路径。换言之，这条路径所包含的边数最小。</p>
<p><strong><em>e.g.</em></strong> <a
href="https://blog.csdn.net/saltriver/article/details/54428983">BFS(图论)图解</a></p>
<p>以邻接表为例：</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//假设图的边有权值</span></span><br><span class="line">vector&lt;Node&gt;Edge[MAX];					        <span class="comment">//邻接表</span></span><br><span class="line"><span class="keyword">bool</span> visited[MAX];						<span class="comment">//存储节点是否被访问过</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Bfs</span><span class="params">(<span class="keyword">int</span> start)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;<span class="keyword">int</span>&gt; q;						<span class="comment">//访问队列</span></span><br><span class="line">    q.<span class="built_in">push</span>(start);</span><br><span class="line">    visited[start]=<span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> now=q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[now].<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> next=Edge[now][i].end;</span><br><span class="line">            <span class="keyword">if</span>(visited[next]==<span class="literal">false</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                q.<span class="built_in">push</span>(next);</span><br><span class="line">                visited[next]=<span class="literal">true</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="复杂度-4">复杂度</h4>
<p>时间复杂度<strong><em>O(M+N)</em></strong>,空间复杂度<strong><em>O(N)</em></strong>。</p>
<h2 id="相关题目">4.相关题目</h2>
<ol type="1">
<li><p><a
href="https://www.luogu.com.cn/problem/P5318">洛谷P5318</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P3916">洛谷P3916</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P1330">洛谷P1330</a></p></li>
</ol>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: 2022-1-27</p>
]]></content>
      <categories>
        <category>Data Structrue</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>图论</tag>
      </tags>
  </entry>
  <entry>
    <title>树的基础知识</title>
    <url>/2022/02/02/%E6%A0%91%E7%9A%84%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/</url>
    <content><![CDATA[<h1 id="树">树</h1>
<h2 id="树的定义">1.1树的定义</h2>
<ul>
<li><p>一棵树是一些<strong>节点(node)</strong>的集合。这个集合可以是空集：若不是空集，则树由称作<strong>根(root)</strong>的节点<strong><em>r</em></strong>以及0个或多个非空的(子)树<strong><em><span
class="math inline">\(T_1,T_2,...,T_K\)</span></em></strong>组成，这些子树中每一颗的根都被来自根<strong><em>r</em></strong>的一条有向的<strong>边(edge)</strong>所连接。</p></li>
<li><p>每一颗子树的根叫作根<strong><em>r</em></strong>的<strong>儿子(child)</strong>,而<strong><em>r</em></strong>是每一颗子树的根的<strong>父亲(parent)</strong>。一棵树是<em>N</em>个节点和<em>N-</em>1条边的集合，其中的一个节点叫作根：每条边都将某个节点连接到它的父亲，而除去根节点外每一个节点都有一个父亲。</p>
<ul>
<li><p>每一个节点可以有任意多个儿子，也可以是零个儿子。没有儿子的节点称为<strong>树叶(leaf)</strong>。</p></li>
<li><p>具有相同父亲的节点称为<strong>兄弟(siblings)</strong>，类似方法可以定义<strong>祖父(grandparent)</strong>和<strong>孙子(granchild)</strong>。</p></li>
</ul></li>
<li><p>从节点<strong><em><span
class="math inline">\(n_1\)</span></em></strong>到<strong><em><span
class="math inline">\(n_k\)</span></em></strong>的路径定义为节点<em><span
class="math inline">\(n_1,n_2,...,n_k\)</span></em>的一个序列，使得对于<em><span
class="math inline">\(1\)</span></em><span
class="math inline">\(\leq\)</span><em><span
class="math inline">\(i\)</span></em><span
class="math inline">\(&lt;\)</span><em><span
class="math inline">\(k\)</span></em>的节点<em><span
class="math inline">\(n_i\)</span></em>是<em><span
class="math inline">\(n_{i+1}\)</span></em>的父亲。这条路径的长是该路径上的边的条数，即<em>k</em>-1。从每一个节点到它自己有一条长为0的路径，在一棵树中从根到每个节点恰好存在一条路径。</p></li>
<li><p>对于任意节点<em><span
class="math inline">\(n_i\)</span></em>，<em><span
class="math inline">\(n_i\)</span></em>的<strong>深度(depth)</strong>为根到<em><span
class="math inline">\(n_i\)</span></em>的唯一路径的长，因此，根的深度为0。</p></li>
<li><p>节点<em><span
class="math inline">\(n_i\)</span></em>的<strong>高(height)</strong>是从<em><span
class="math inline">\(n_i\)</span></em>到一片树叶的最长路径的长。因此所有树叶的高都是0。一颗树的<strong>树高(height
of a tree)</strong>等于它的根的高。</p>
<ul>
<li>一棵树的<strong>树深(depth of a
tree)</strong>等于其最深的树叶的深度，该深度总是等于这棵树的高。</li>
</ul></li>
<li><p>如果存在从<em><span
class="math inline">\(n_1\)</span></em>到<em><span
class="math inline">\(n_2\)</span></em>的一条路径，那么<em><span
class="math inline">\(n_1\)</span></em>是<em><span
class="math inline">\(n_2\)</span></em>的一位<strong>祖先(ancestor)</strong>，而<em><span
class="math inline">\(n_2\)</span></em>是<em><span
class="math inline">\(n_1\)</span></em>的一个<strong>后裔(descendant)</strong>。如果<em><span
class="math inline">\(n_1 \neq n_2\)</span></em>，那么<em><span
class="math inline">\(n_1\)</span></em>是<em><span
class="math inline">\(n_2\)</span></em>的<strong>真祖先(proper
ancestor)</strong>，而<em><span
class="math inline">\(n_2\)</span></em>是<em><span
class="math inline">\(n_1\)</span></em>的<strong>真后裔(proper
descendant)</strong>。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-02%20162632.png" /></p>
<center>
<p>
<p>树的基本关系</p>
</p>
<center>
<hr />
<p><strong>P.S</strong> 树与图论的相关联系</p></li>
<li><p>一个没有固定根节点的树称为<strong>无根树(unrooted
tree)</strong>，在无根树基础上，指定一个节点为<strong>根</strong>，则形成一颗<strong>有根树(rooted
tree)</strong>，无根树有几种等价形式化定义：</p>
<ul>
<li>有<em><span class="math inline">\(n\)</span></em>个节点，<em><span
class="math inline">\(n-1\)</span></em>条边的连通无向图</li>
<li>无向无环的连通图</li>
<li>任意两个节点之间有且仅有一条简单路径的无向图</li>
<li>任何边均为桥的连通图</li>
<li>没有圈，且在任意不同两点间添加一条边之后所得图含唯一的一个圈的图</li>
</ul></li>
<li><p><strong>森林(forest)</strong>：每个连通分量(连通块)都是树的图，按照定义，一棵树也是森林。</p></li>
<li><p><strong>生成树(spanning
tree)</strong>：一个连通无向图的生成子图，同时要求是图。也即在图的边集中选择<em><span
class="math inline">\(n-1\)</span></em>条，将所有顶点连通。</p></li>
<li><p><strong>无根树的叶节点(leaf node)</strong>：度数不超过<em><span
class="math inline">\(1\)</span>​​</em>的节点。</p></li>
</ul>
<h2 id="树的实现">1.2树的实现</h2>
<h3 id="只记录父节点">只记录父节点</h3>
<p>用一个数组<strong>parent[N]</strong>记录每个节点的父亲节点。</p>
<p>缺陷：这种方式可以获得的信息较少，不便于进行自顶向下的遍历。常用于自底向上的递推问题中。</p>
<h3 id="邻接表">邻接表</h3>
<ul>
<li><p>对于无根树：为每个节点开辟一个线性列表，记录所有与之相连的节点。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="keyword">int</span>&gt; Tree[N];		<span class="comment">//N为所有节点个数</span></span><br></pre></td></tr></table></figure></li>
<li><p>对于有根树：</p>
<ul>
<li><p>若输入数据可确定节点的关系，则为每个节点开辟一个线性列表，记录所有子节点；若有需要，则在另一个数组中记录父节点。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line">vector&lt;<span class="keyword">int</span>&gt;child[N];</span><br><span class="line"><span class="keyword">int</span> parent[N];</span><br></pre></td></tr></table></figure></li>
</ul></li>
</ul>
<h3 id="双亲表示法">双亲表示法</h3>
<p>给每个节点的所有子节点任意确定一个顺序。此后为每个节点记录两个值：其<strong>第一个子节点</strong>和<strong>下一个兄弟节点</strong>。若没有子节点，则子节点为空；若该节点为父节点的最后一个子节点，则下一个兄弟节点为空。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-02%20172002.png" /></p>
<center>
<p>
树的双亲表示法
</p>
</center>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">TreeNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    Object element;			<span class="comment">//存储的数据</span></span><br><span class="line">    TreeNode*firstchild;	<span class="comment">//第一个子节点</span></span><br><span class="line">    TreeNode*nextSibling;	<span class="comment">//下一个兄弟节点</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>数组实现:</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Node</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> parent;				<span class="comment">//存储父节点</span></span><br><span class="line">    <span class="keyword">int</span> child;				<span class="comment">//存储首个子节点</span></span><br><span class="line">    <span class="keyword">int</span> sibling;			<span class="comment">//存储下一个兄弟节点</span></span><br><span class="line">&#125;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">Node</span> <span class="title">Tree</span>[<span class="title">MAX</span>];</span></span><br><span class="line"><span class="comment">//或者</span></span><br><span class="line"><span class="keyword">int</span> parent[MAX];</span><br><span class="line"><span class="keyword">int</span> child[MAX];</span><br><span class="line"><span class="keyword">int</span> sibling[MAX];</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="树的遍历">1.3树的遍历</h2>
<h3 id="树的dfs">树的DFS</h3>
<p>在树上 DFS
是这样的一个过程：先访问根节点，然后分别访问根节点每个儿子的子树。</p>
<p>可以用来求出每个节点的深度、父亲等信息。</p>
<h3 id="树的bfs">树的BFS</h3>
<p>从树根开始，严格按照层次来访问节点。</p>
<p>BFS 过程中也可以顺便求出各个节点的深度和父亲节点。</p>
<h3 id="无根树的遍历">无根树的遍历</h3>
<p>无根树的遍历一般为深度优先遍历，这个过程中最需要注意的是避免重复访问节点。</p>
<p>由于树是无环图，因此只需记录当前节点是由哪个节点访问而来，此后进入除该节点外的所有相邻节点，即可避免重复访问。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">bool</span> visited[MAX];</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Dfs</span><span class="params">(<span class="keyword">int</span> start)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    visited[start]=<span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Tree[start].<span class="built_in">size</span>();i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> next=Tree[start][i];</span><br><span class="line">        <span class="keyword">if</span>(visited[next]==<span class="literal">false</span>)</span><br><span class="line">            <span class="built_in">Dfs</span>(next);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="有根树的遍历">有根树的遍历</h3>
<p>有根树的遍历与无根树的遍历相同，只是注意区分节点的上下关系。</p>
<h2 id="特殊的树">1.4特殊的树</h2>
<ul>
<li><p><strong>链(chain/path
graph)</strong>：满足与任一节点相连的边不超过<em><span
class="math inline">\(2\)</span></em>条的树称为链。</p></li>
<li><p><strong>菊花/星星(star)</strong>：满足存在<em><span
class="math inline">\(u\)</span></em>使得所有除<em><span
class="math inline">\(u\)</span></em>以外节点均与<em><span
class="math inline">\(u\)</span></em>相连的树称为菊花。</p></li>
<li><p><strong>有根二叉树（rooted binary
tree）</strong>：每个节点最多只有两个儿子（子节点）的有根树称为二叉树。常常对两个子节点的顺序加以区分，分别称之为左子节点和右子节点。大多数情况下，<strong>二叉树</strong>
一词均指有根二叉树。</p>
<ul>
<li><p><strong>完整二叉树（full/proper binary
tree）</strong>：除叶子节点外每个节点都有两个孩子。</p></li>
<li><p><strong>完美二叉树（perfect binary
tree）</strong>：所有内部节点都有两个子节点，且所有叶子节点位于同一层。</p>
<p><img src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/perfect-binary-tree_0.png" style="zoom: 67%;" /></p>
<center>
<p>
<p>完美二叉树</p>
</p>
</center></li>
<li><p><strong>完全二叉树（complete binary
tree）</strong>：若设二叉树的深度为<em><span
class="math inline">\(h\)</span></em>，除第 <em><span
class="math inline">\(h\)</span></em> 层外，其它各层 (1～<em><span
class="math inline">\(h-1\)</span></em>) 的节点数都达到最大个数，第
<em><span class="math inline">\(h\)</span></em>
层所有的节点都连续集中在最左边，这就是完全二叉树。</p>
<p>特点：</p>
<ol type="1">
<li>所有叶子节点必须向左倾斜</li>
<li>最后一个叶子节点可能没有右兄弟(<strong>P.S</strong>
完全二叉树(complete binary tree)不一定是完整二叉树(full binary tree))
<figure class="half">
<img src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/comparison-1_0.png">
<img src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/comparison-4.png">
</figure></li>
</ol></li>
</ul></li>
</ul>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: 2022-2-02</p>
]]></content>
      <categories>
        <category>Data Structrue</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>图论</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>二叉树</title>
    <url>/2022/02/02/%E4%BA%8C%E5%8F%89%E6%A0%91/</url>
    <content><![CDATA[<h1 id="二叉树">二叉树</h1>
<p><strong>二叉树(binary
tree)</strong>是一颗树，其中每个节点都不能有多于两个的儿子。一颗二叉树由一个根和两颗子树<em><span
class="math inline">\(T_L\)</span></em>和<em><span
class="math inline">\(T_R\)</span></em>组成，<em><span
class="math inline">\(T_L\)</span></em>和<em><span
class="math inline">\(T_R\)</span></em>均可能为空。</p>
<p>二叉树的第<em><span
class="math inline">\(i\)</span></em>层最多拥有<em><span
class="math inline">\(2^{i-1}\)</span></em>个节点，深度为<em><span
class="math inline">\(k\)</span></em>的二叉树最多总共有<em><span
class="math inline">\(2^{k+1}-1\)</span></em>个节点。</p>
<p>二叉树可分为：<strong>完整二叉树</strong>、<strong>完全二叉树</strong>、<strong>完美二叉树</strong>。</p>
<p><a
href="https://asukamilet.github.io/2022/02/02/%E6%A0%91%E7%9A%84%E5%9F%BA%E7%A1%80%E7%9F%A5%E8%AF%86/">二叉树的分类</a></p>
<hr />
<h2 id="二叉树的构造">1.二叉树的构造</h2>
<h3 id="数组存储">数组存储</h3>
<p>二叉树可以用数组或链表来存储，若为满二叉树就可紧凑排列。若某个节点的索引为<em><span
class="math inline">\(i\)</span></em>(假设根节点的索引为0)则它左子节点的索引为<em><span
class="math inline">\(2i+1\)</span></em>，右子节点的索引为<em><span
class="math inline">\(2i+2\)</span></em>，父节点(若有)的索引为<em><span
class="math inline">\(\lfloor {i-1}/2 \rfloor\)</span></em></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> parent;</span><br><span class="line">    <span class="keyword">int</span> left;</span><br><span class="line">    <span class="keyword">int</span> right;</span><br><span class="line">&#125;Node;</span><br><span class="line">Node BinaryTree[MAX];</span><br></pre></td></tr></table></figure>
<h3 id="链表存储">链表存储</h3>
<p>如果一个节点的子节点个数小于2，一些子节点指针可能为空值。使用链表能避免顺序存储浪费空间的问题，算法与结构相对简单，但使用链表，由于缺乏父链的指引，在找回父节点时需要重新扫描树得知父节点的节点地址。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BinaryNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    Object element;			<span class="comment">//节点上的数据</span></span><br><span class="line">    BinaryNode *left;		<span class="comment">//左儿子</span></span><br><span class="line">    BinaryNode *right;		<span class="comment">//右儿子</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">BinaryNode</span> *<span class="title">BinaryTree</span>;</span></span><br></pre></td></tr></table></figure>
<h2 id="二叉树的遍历">2.二叉树的遍历</h2>
<h3 id="深度优先搜索dfs">2.1深度优先搜索(DFS)</h3>
<p>在深度优先搜索中，我们希望从根节点访问最远的节点。和图的深度优先搜索不同的是，不需记住访问过的每一个节点，因为树中不会有环。前序，中序和后序遍历都是深度优先遍搜索的特例。</p>
<h4 id="前序遍历">2.1.1前序遍历</h4>
<p>按照<strong>根、左、右</strong>的顺序遍历二叉树</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PreTrav</span><span class="params">(BinaryTree*root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">//相关操作</span></span><br><span class="line">        <span class="built_in">PreTrav</span>(root-&gt;left);</span><br><span class="line">        <span class="built_in">PreTrav</span>(root-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="中序遍历">2.1.2中序遍历</h4>
<p>按照<strong>左、根、右</strong>的顺序遍历二叉树</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">MidTrav</span><span class="params">(BinaryTree*root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">MidTrav</span>(root-&gt;left);</span><br><span class="line">        <span class="comment">//相关操作</span></span><br><span class="line">        <span class="built_in">MidTrav</span>(root-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="后序遍历">2.1.2后序遍历</h4>
<p>按照<strong>左、右、根</strong>的顺序遍历二叉树</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">LastTrav</span><span class="params">(BinaryTree*root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(root!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">LastTrav</span>(root-&gt;left);</span><br><span class="line">        <span class="built_in">LastTrav</span>(root-&gt;right);</span><br><span class="line">        <span class="comment">//相关操作</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="广度优先搜索bfs">2.2广度优先搜索(BFS)</h3>
<p>从树根开始，严格按照层次来访问节点，BFS过程中也可顺便求出各个节点的深度以及父亲节点。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//本质还是BFS</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">LevelTrav</span><span class="params">(BinaryTree*root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;BinaryNode*&gt; q;	<span class="comment">//树节点指针的队列</span></span><br><span class="line">    q.<span class="built_in">push</span>(root);</span><br><span class="line">    <span class="keyword">while</span>(！q.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        BinaryNode*now=q.<span class="built_in">front</span>();</span><br><span class="line">        <span class="comment">//相关操作，如输出存储数据</span></span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">if</span>(now-&gt;left!=<span class="literal">nullptr</span>)	<span class="comment">//q.front()得到左儿子</span></span><br><span class="line">            q.<span class="built_in">push</span>(now-&gt;left);</span><br><span class="line">        <span class="keyword">if</span>(now-&gt;right!=<span class="literal">nullptr</span>)	<span class="comment">//q.front()得到右儿子</span></span><br><span class="line">            q.<span class="built_in">push</span>(now-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>二叉树遍历会对树的每个节点进行一次访问，算法复杂度为<strong><em>O(N)</em></strong>，使用递归实现遍历时，一旦树的节点数量庞大且分布不均，可能导致递归深度过深。</p>
<h2 id="二叉树的应用">3.二叉树的应用</h2>
<p>二叉树的一个重要应用即它在搜索中的使用，赋予二叉树相关性质即可使二叉树成为二叉查找树</p>
<p>使二叉树成为二叉查找树的性质是，对于树中每个节点<strong><em>X</em></strong>，它的左子树中所有项的值均小于<strong><em>X</em></strong>中的项，而它右子树中所有项的值均大于<strong><em>X</em></strong>中的项。这意味着该树所有的元素可以用某种一致的方式排序。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-03%20155815.png" /></p>
<center>
<p>
两颗二叉树(只有左边的树为查找树)
</p>
</center>
<h3 id="二叉查找树的相关操作">3.1二叉查找树的相关操作</h3>
<h4 id="数组实现">3.1.1数组实现</h4>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">TreeNode</span>					//构建<span class="title">BST</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> value;</span><br><span class="line">    <span class="keyword">int</span> left;					<span class="comment">//当left与right的值为-1时，表示无子节点</span></span><br><span class="line">    <span class="keyword">int</span> right;</span><br><span class="line">&#125;;</span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">TreeNode</span> <span class="title">TreeNode</span>;</span></span><br><span class="line">TreeNode BStree[MAX];</span><br><span class="line"><span class="comment">//BStree[0]即为根节点</span></span><br></pre></td></tr></table></figure>
<h5 id="遍历二叉搜索树">遍历二叉搜索树</h5>
<p>BST的中序遍历是其所有节点值从小到大排列，时间复杂度为<strong><em>O(N)</em></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Traverse</span><span class="params">(<span class="keyword">int</span> root)</span>			<span class="comment">//遍历以root为根节点的BST</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">	<span class="keyword">if</span>(BStree[root].left!=<span class="number">-1</span>)	<span class="comment">//当左子树不为空时，遍历左子树</span></span><br><span class="line">        <span class="built_in">Traverse</span>(BStree[root].left);</span><br><span class="line">    cout&lt;&lt;BStree[root].value;	<span class="comment">//输出根节点的值</span></span><br><span class="line">    <span class="keyword">if</span>(BStree[root].right!=<span class="number">-1</span>)	<span class="comment">//当右子树不为空时，遍历右子树</span></span><br><span class="line">        <span class="built_in">Traverse</span>(BStree[root].right);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="查找最小最大值">查找最小/最大值</h5>
<p>由BST的性质可知，BST上最小值位于BST最左端顶点，最大值位于BST最右端顶点。时间复杂度为<strong><em>O(<span
class="math inline">\(h\)</span>)</em></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">FindMin</span><span class="params">(<span class="keyword">int</span> root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(BStree[root].left!=<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">FindMin</span>(BStree[root].left);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> BStree[root].value;</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">FindMax</span><span class="params">(<span class="keyword">int</span> root)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(BStree[root].right!=<span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">FindMax</span>(BStree[root].right);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> BStree[root].value;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="指针实现">3.1.2 指针实现</h4>
<p>由于BST的平均深度为<strong><em>O(<span
class="math inline">\(\log{N}\)</span>)</em></strong>，所以一般不必担心栈空间会被用尽</p>
<h5 id="bst接口">BST接口</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BinarySearchTree</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">BinarySearchTree</span>();</span><br><span class="line">    <span class="built_in">BinarySearchTree</span>(<span class="keyword">const</span> BinarySearchTree&amp;rhs);</span><br><span class="line">    <span class="built_in">BinarySearchTree</span>(BinarySearchTree&amp;&amp;rhs);</span><br><span class="line">    ~<span class="built_in">BinarySearchTree</span>();</span><br><span class="line">    <span class="comment">//构造函数</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">const</span> T&amp;<span class="title">findMin</span><span class="params">()</span> <span class="keyword">const</span></span>;		 <span class="comment">//查找最小值</span></span><br><span class="line">    <span class="function"><span class="keyword">const</span> T&amp;<span class="title">findMax</span><span class="params">()</span> <span class="keyword">const</span></span>;		 <span class="comment">//查找最大值</span></span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">contains</span><span class="params">(<span class="keyword">const</span> T&amp;x)</span> <span class="keyword">const</span></span>;  <span class="comment">//判断是否含有项X的节点</span></span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">empty</span><span class="params">()</span> <span class="keyword">const</span></span>;						 <span class="comment">//判断是否为空树</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">printTree</span><span class="params">(ostream&amp;out=cout)</span> <span class="keyword">const</span></span>;	 <span class="comment">//输出树</span></span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">makeEmpty</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">const</span> T&amp;x)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(T&amp;&amp;x)</span></span>;</span><br><span class="line">    <span class="comment">//插入元素</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">remove</span><span class="params">(<span class="keyword">const</span> T&amp;x)</span></span>;			 <span class="comment">//删除元素，delete为关键字，用remove</span></span><br><span class="line">    </span><br><span class="line">    BinarySearchTree&amp;<span class="keyword">operator</span>=(<span class="keyword">const</span> BinarySearchTree&amp;rhs);</span><br><span class="line">    BinarySearchTree&amp;<span class="keyword">operator</span>=(BinarySearchTree&amp;&amp;rhs);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">BinaryNode</span></span></span><br><span class="line"><span class="class">    &#123;</span></span><br><span class="line">        T element;</span><br><span class="line">        BinaryNode*left;</span><br><span class="line">        BinaryNode*right;</span><br><span class="line">        <span class="comment">//BinaryNode*parent;		 父节点可加可不加</span></span><br><span class="line">        </span><br><span class="line">        <span class="built_in">BinaryNode</span>(<span class="keyword">const</span> T&amp;value,BinaryNode*lc,BinaryNode*rc)</span><br><span class="line">            :element&#123;value&#125;,left&#123;lc&#125;,right&#123;rc&#125; &#123;&#125;</span><br><span class="line">        <span class="built_in">BinaryNode</span>(T&amp;&amp;value,BinaryNode*lc,BinaryNode*rc)</span><br><span class="line">            :element&#123;std::<span class="built_in">move</span>(value)&#125;,left&#123;lc&#125;,right&#123;rc&#125; &#123;&#125;</span><br><span class="line">        <span class="comment">//节点的构造函数</span></span><br><span class="line">    &#125;;</span><br><span class="line">    </span><br><span class="line">    BinaryNode*root;				 <span class="comment">//定义根节点</span></span><br><span class="line">    </span><br><span class="line">    <span class="comment">//内部重载函数</span></span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(BinaryNode*&amp;t,<span class="keyword">const</span> T&amp;x)</span></span>;	</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(BinaryNode*&amp;t,T&amp;&amp;x)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">remove</span><span class="params">(BinaryNode*&amp;t,<span class="keyword">const</span> T&amp;x)</span></span>;</span><br><span class="line">    <span class="function">BinaryNode*<span class="title">findMax</span><span class="params">(BinaryNode*t)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function">BinaryNode*<span class="title">findMin</span><span class="params">(BinaryNode*t)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">bool</span> <span class="title">contains</span><span class="params">(BinaryNode*t,<span class="keyword">const</span> T&amp;x)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">makeEmpty</span><span class="params">(BinaryNode*&amp;t)</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">printTree</span><span class="params">(BinaryNode*t,ostream&amp;out)</span> <span class="keyword">const</span></span>;</span><br><span class="line">    <span class="function">BinaryNode*<span class="title">clone</span><span class="params">(BinaryNode*t)</span> <span class="keyword">const</span></span>;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<center>
<p>
二叉查找树接口
</p>
</center>
<p><em>private</em>成员函数通过<strong>传引用调用</strong>的使用传递指针变量，使得public型成员函数能够把指向根节点的指针传递给<em>private</em>型的递归成员函数，这些递归函数此时可以改变根的值，使<em>root</em>指向另外的节点。</p>
<h5 id="private与public关系">private与public关系</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="keyword">bool</span> BinarySearchTree&lt;T&gt;::<span class="built_in">contains</span>(<span class="keyword">const</span> T&amp;x) <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">contains</span>(x,root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> BinarySearchTree&lt;T&gt;::<span class="built_in">insert</span>(<span class="keyword">const</span> T&amp;x)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">insert</span>(x,root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> BinarySearchTree&lt;T&gt;::<span class="built_in">remove</span>(<span class="keyword">const</span> T&amp;x)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">remove</span>(x,root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> T&amp;BinarySearchTree&lt;T&gt;::<span class="built_in">findMin</span>() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">findMin</span>(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> T&amp;BinarySearchTree&lt;T&gt;::<span class="built_in">findMax</span>() <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="built_in">findMax</span>(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> BinarySearchTree&lt;T&gt;::<span class="built_in">makeEmpty</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">makeEmpty</span>(root);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">void</span> BinarySearchTree&lt;T&gt;::<span class="built_in">printTree</span>(ostream&amp;out=cout) <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">printTree</span>(root,cout);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="contains">contains</h5>
<p>如果在树<span class="math inline">\(T\)</span>中含有项<span
class="math inline">\(X\)</span>的节点，则该此操作返回<em>true</em>，若该节点不存在，则返回<em>false</em>。若<span
class="math inline">\(T\)</span>为空集，则直接返回<em>false</em>，否则，若存储在<span
class="math inline">\(T\)</span>的项为<span
class="math inline">\(X\)</span>，则返回<em>true</em>；不然对树<span
class="math inline">\(T\)</span>的左子树和右子树进行递归调用。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment">*判断某项是否在子树上方法</span></span><br><span class="line"><span class="comment">*x为待查找项</span></span><br><span class="line"><span class="comment">*t为作为该子树的根节点</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">contains</span><span class="params">(<span class="keyword">const</span> T&amp;x,BinaryNode*t)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(t==<span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(x&lt;t-&gt;element)</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">contains</span>(x,t-&gt;left);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(x&gt;t-&gt;element)</span><br><span class="line">        <span class="keyword">return</span> <span class="built_in">contains</span>(x,t-&gt;right);</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<center>
<p>
二叉查找树的contains操作
</p>
</center>
<h5 id="findmax和findmin">findMax和findMin</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function">BinaryNode*<span class="title">findMin</span><span class="params">(BinaryNode*t)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(t!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">while</span>(t-&gt;left!=<span class="literal">nullptr</span>)</span><br><span class="line">            t=t-&gt;left;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">//private成员函数</span></span><br><span class="line"><span class="function">BinaryNode*<span class="title">findMax</span><span class="params">(BinaryNode*t)</span> <span class="keyword">const</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(t!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">while</span>(t-&gt;right!=<span class="literal">nullptr</span>)</span><br><span class="line">            t=t-right;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> t;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="insert">insert</h5>
<p>为了将<span class="math inline">\(X\)</span>插入到树<span
class="math inline">\(T\)</span>中，可以沿着树进行查找，如果找到<span
class="math inline">\(X\)</span>,则什么也不用做。否则将<span
class="math inline">\(X\)</span>插入至所遍历路径的最后一点上。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-09%20230457.png" /></p>
<center>
<p>
插入5之前和之后的BST
</p>
</center>
<p>重复元的插入可以通过在节点中保留一个<code>count</code>计数器来记录该值在树中出现的次数。也可把具有相同关键字的所有结构保留在一个辅助数据结构中，例如表或者另一颗BST。</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//x为待插入的项，t为该子树根节点</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">insert</span><span class="params">(<span class="keyword">const</span> T&amp;x,BinaryNode*&amp;t)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(t==<span class="literal">nullptr</span>)</span><br><span class="line">        t=<span class="keyword">new</span> BinaryNode&#123;x,<span class="literal">nullptr</span>,<span class="literal">nullptr</span>&#125;;	<span class="comment">//列表初始化</span></span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(x&lt;t-&gt;element)</span><br><span class="line">        <span class="built_in">insert</span>(x,t-&gt;left);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(x&gt;t-&gt;element)</span><br><span class="line">        <span class="built_in">insert</span>(x,t-&gt;right);</span><br><span class="line">    <span class="keyword">else</span> </span><br><span class="line">        (t-&gt;count)++;							<span class="comment">//重复元，计数器+1</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="remove">remove</h5>
<ul>
<li>若待删除节点为树叶，则立即被删除</li>
<li>若待删除节点有一个儿子，则该节点在其父节点调整其链绕过该节点后被删除<img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-10%20145457.png" /></li>
</ul>
<center>
<p>
有一个儿子的节点被删除情况
</p>
</center>
<ul>
<li>若待删除节点有两个儿子，一般用其右子树的最小数据代替该节点数据并递归删除那个节点<img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-10%20145733.png" /></li>
</ul>
<center>
<p>
有两个儿子节点前后情况
</p>
</center>
<ul>
<li>若预计删除次数不多，则可以使用<strong>懒惰删除(lazy
deletion)</strong>：当一个元素要被删除时，它仍留在树中，只是做了一个被删除的标记。</li>
</ul>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">remove</span><span class="params">(BinaryNode*&amp;t,<span class="keyword">const</span> T&amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span>(t==<span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span>(x&lt;t-&gt;element)</span><br><span class="line">        <span class="built_in">remove</span>(t-&gt;left,x);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(x&gt;t-&gt;element)</span><br><span class="line">        <span class="built_in">remove</span>(t-&gt;right,x);</span><br><span class="line">    <span class="keyword">else</span> <span class="keyword">if</span>(t-&gt;left!=<span class="literal">nullptr</span>&amp;&amp;t-&gt;right!=<span class="literal">nullptr</span>)	<span class="comment">//待删除节点有两个儿子</span></span><br><span class="line">    &#123;</span><br><span class="line">        t-&gt;element=<span class="built_in">findMin</span>(t-&gt;right)-&gt;element;		<span class="comment">//右子树最小值</span></span><br><span class="line">        <span class="built_in">remove</span>(t-&gt;element,t-&gt;right);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span>		<span class="comment">//待删除节点只有一个儿子</span></span><br><span class="line">    &#123;</span><br><span class="line">        BinaryNode*oldNode=t;</span><br><span class="line">        <span class="keyword">if</span>(t-&gt;left!=<span class="literal">nullptr</span>)</span><br><span class="line">            t=t-&gt;left;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            t=t-&gt;right;		<span class="comment">//待删除节点被其儿子代替</span></span><br><span class="line">        <span class="keyword">delete</span> oldNode;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="析构函数与构造拷贝函数">析构函数与构造拷贝函数</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="comment">//析构函数与makeEmpty</span></span><br><span class="line">BinarySearchTree&lt;T&gt;::~<span class="built_in">BinarySearchTree</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">makeEmpty</span>();</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">void</span> BinarySearchTree&lt;T&gt;::<span class="built_in">makeEmpty</span>(BinaryNode*&amp;t)</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span>(t!=<span class="literal">nullptr</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">makeEmpty</span>(t-&gt;left);</span><br><span class="line">        <span class="built_in">makeEmpty</span>(t-&gt;right);</span><br><span class="line">        <span class="keyword">delete</span> t;</span><br><span class="line">    &#125;</span><br><span class="line">    t=<span class="literal">nullptr</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">//构造函数</span></span><br><span class="line">BinarySearchTree&lt;T&gt;::<span class="built_in">BinarySearchTree</span>(<span class="keyword">const</span> BinarySearchTree&amp;rhs): root&#123;<span class="literal">nullptr</span>&#125;</span><br><span class="line">&#123;</span><br><span class="line">    root=<span class="built_in">clone</span>(rhs.root);</span><br><span class="line">&#125;</span><br><span class="line">BinaryNode*BinarySearchTree&lt;T&gt;::<span class="built_in">clone</span>(BinaryNode*t) <span class="keyword">const</span></span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">if</span>(t==<span class="literal">nullptr</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">nullptr</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="keyword">new</span> BinaryNode&#123;t-&gt;element,<span class="built_in">clone</span>(t-&gt;left),<span class="built_in">clone</span>(t-&gt;right)&#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: F2022-2-02 L2022-2-10</p>
]]></content>
      <categories>
        <category>Data Structrue</category>
      </categories>
      <tags>
        <tag>数据结构</tag>
        <tag>树</tag>
      </tags>
  </entry>
  <entry>
    <title>图的最短路问题</title>
    <url>/2022/02/22/%E5%9B%BE%E7%9A%84%E6%9C%80%E7%9F%AD%E8%B7%AF/</url>
    <content><![CDATA[<h1 id="最短路定义">最短路定义</h1>
<p>最短路是给定两个节点，在以这两个点为起点和终点的路径中，边的权值和最小的路径。</p>
<ul>
<li>若输入的为一个赋权图，与每条边(<span
class="math inline">\(v_i,v_j\)</span>)相联系的是穿越该边的开销(或称为值)<span
class="math inline">\(c_{i,j}\)</span>。一条路径<span
class="math inline">\(v_1v_2...v_N\)</span>的值为<span
class="math inline">\(\sum^{N-1}_{i=1}{c_{i,j}}\)</span>，称为<strong>赋权路径长(weighted
path length)</strong>。</li>
<li><strong>无权路径长(unweighted path
length)</strong>只是路径上的边数，即<span
class="math inline">\(N-1\)</span>​。</li>
</ul>
<hr />
<h1 id="最短路性质">最短路性质</h1>
<ol type="1">
<li>对于边权为正的图，任意两个节点之间的最短路，不会经过重复的节点</li>
<li>对于边权为正的图，任意两个节点之间的最短路，不会经过重复的边</li>
<li>对于边权为正的图，任意两个节点之间的最短路，任意一条的节点数不会超过<span
class="math inline">\(n\)</span>，边数不会超过<span
class="math inline">\(n-1\)</span></li>
</ol>
<hr />
<h1 id="最短路相关问题">最短路相关问题</h1>
<h2 id="单源最短路径问题">单源最短路径问题</h2>
<p>定义：固定一个起点<span
class="math inline">\(s\)</span>，求它到图<span
class="math inline">\(G\)</span>中其他所有点的最短路问题。</p>
<p>当图中的边存在负值时，可以任意长地滞留在图中，使路径长<span
class="math inline">\(D(u)\to-\infty\)</span>。此时路径形成一个循环，称为<strong>负值圈(negative-cost
cycle)</strong>。当它出现在图中时，最短路径问题就是不确定的，在没有负值圈时，从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(s\)</span>的最短路径为0。</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/J3QVM.png" /></p>
<center>
<p>
带有负值圈的图
</p>
</center>
<hr />
<h3 id="无权最短路径">无权最短路径</h3>
<p>可以使用<strong>广度优先搜索</strong>，该方法按层处理顶点：距开始点最近的那些顶点首先被求值，而最远的那些顶点最后被求值，类似于树的层序遍历。若使用邻接表，时间复杂度为<strong><em>O(<span
class="math inline">\(|E|+|V|\)</span>)</em></strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> INFINITY 999999(足够大，保证达不到这个数)</span></span><br><span class="line"><span class="comment">/**对每一个节点V，在执行Insert函数时设置距离</span></span><br><span class="line"><span class="comment">void Insert(int v,int w)</span></span><br><span class="line"><span class="comment">&#123;</span></span><br><span class="line"><span class="comment">	dist[v]=INFINITY;</span></span><br><span class="line"><span class="comment">	Edge[v].push_back(w);</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">UnweightedPath</span><span class="params">(<span class="keyword">int</span> start)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;<span class="keyword">int</span>&gt; q;</span><br><span class="line">    dist[start]=<span class="number">0</span>;		<span class="comment">//将起始点距离设为0</span></span><br><span class="line">    q.<span class="built_in">push</span>(start);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> now=q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[now].<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> next=Edge[now][i];</span><br><span class="line">            <span class="keyword">if</span>(dist[next]==INFINITY)		<span class="comment">//未访问到next节点</span></span><br><span class="line">            &#123;</span><br><span class="line">                dist[next]=dist[now]+<span class="number">1</span>;</span><br><span class="line">                <span class="comment">//记录当前节点与起点的距离</span></span><br><span class="line">                path[next]=now;</span><br><span class="line">                <span class="comment">//记录具体路径，即访问当前节点之前的节点是哪一个</span></span><br><span class="line">                q.<span class="built_in">push</span>(next);</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/%E5%B1%8F%E5%B9%95%E6%88%AA%E5%9B%BE%202022-02-14%20151137.png" /></p>
<center>
<p>
无权最短路径算法数据变化情况
</p>
</center>
<h3 id="赋权最短路径">赋权最短路径</h3>
<hr />
<p><strong>松弛操作</strong>:如果存在一条从<span
class="math inline">\(u\)</span>到<span
class="math inline">\(v\)</span>​的边，那么从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(v\)</span>的一条新路径是将边<span
class="math inline">\(e(u,v)\in E\)</span>添加到从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(u\)</span>的路径尾部来拓展一条从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(v\)</span>的路径，这条路径长为<span
class="math inline">\(distance(u)+w(u,v)\)</span>。如果这个值比目前已知的<span
class="math inline">\(distance(v)\)</span>要小，则可以用这个值来替代当前<span
class="math inline">\(distance(v)\)</span>中的值。松弛边的操作一直执行到任一节点的<span
class="math inline">\(distance(v_i)\)</span>都代表从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(v_i\)</span>的最短路径长度值。</p>
<p><strong>公式</strong>:<span
class="math inline">\(distance(v)\)</span>=<strong><em>min</em></strong>(<span
class="math inline">\(distance(v),distance(u)+w(u,v)\)</span>)。</p>
<hr />
<h4 id="dijkstra算法">Dijkstra算法</h4>
<p>当图中不存在<strong>负的边值</strong>时，可以使用<strong>Dijkstra算法</strong>，<strong>Dijkstra算法</strong>为贪心的一个典型实例。</p>
<p>初始化：<strong>Dijkstra</strong>将节点分为两个集合：已经确定最短路长度的点集(<span
class="math inline">\(S\)</span>)和未确定最短路长度的点集(<span
class="math inline">\(T\)</span>)。最初所有的点均属于集合<span
class="math inline">\(T\)</span>。初始化<span
class="math inline">\(distance(s)=0\)</span>，其他点的<em>distance</em>均设置为<span
class="math inline">\(+\infin\)</span>。</p>
<p>重复操作：</p>
<ol type="1">
<li>从<span
class="math inline">\(T\)</span>集合中，选取一个最短路长度最小的节点<span
class="math inline">\(u\)</span>，移至<span
class="math inline">\(S\)</span>中</li>
<li>当<span class="math inline">\(u\)</span>从<span
class="math inline">\(T\)</span>中转移至<span
class="math inline">\(S\)</span>之后，对<span
class="math inline">\(u\)</span>的每条外接边<span
class="math inline">\(w(u,v)\)</span>进行松弛操作</li>
</ol>
<p>当集合<span class="math inline">\(T\)</span>为空时，算法结束</p>
<p><img
src="https://raw.githubusercontent.com/AsukaMilet/BlogPhotos/master/Typora/Dijkstra_Animation.gif" /></p>
<center>
<p>
Dijkstra算法具体实例
</p>
</center>
<hr />
<h5 id="时间复杂度">时间复杂度</h5>
<p>有多种数据结构来实现<strong>Dijkstra算法</strong>，不同的数据结构实现时间复杂度不同</p>
<table>
<thead>
<tr class="header">
<th style="text-align: center;">数据结构</th>
<th style="text-align: center;">时间复杂度</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td style="text-align: center;">邻接表</td>
<td style="text-align: center;"><strong><em>O(<span
class="math inline">\(|V^2|\)</span>)</em></strong></td>
</tr>
<tr class="even">
<td style="text-align: center;">二叉堆</td>
<td style="text-align: center;"><strong><em>O(<span
class="math inline">\((|E|+|V|)log{|V|}\)</span>)</em></strong></td>
</tr>
<tr class="odd">
<td style="text-align: center;">优先队列</td>
<td style="text-align: center;"><strong><em>O(<span
class="math inline">\(|E|log{|V|}\)</span>)</em></strong></td>
</tr>
<tr class="even">
<td style="text-align: center;">斐波那契堆</td>
<td style="text-align: center;"><strong><em>O(<span
class="math inline">\(|E|+|V|log{|V|}\)</span>)</em></strong></td>
</tr>
</tbody>
</table>
<hr />
<h5 id="算法实现">算法实现</h5>
<h6 id="邻接表无优化实现">1.邻接表无优化实现</h6>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//num为顶点个数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Dijkstra</span><span class="params">(<span class="keyword">int</span> start,<span class="keyword">int</span> num)</span>		</span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dist[start]=<span class="number">0</span>;					<span class="comment">//起始节点初始化为0</span></span><br><span class="line">    <span class="keyword">int</span> u=<span class="number">-1</span>;						<span class="comment">//未知节点中距离最小节点位置</span></span><br><span class="line">    <span class="keyword">int</span> MIN=INFINITY;				<span class="comment">//dist[]中最小值</span></span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span>(visited[i]==<span class="literal">false</span>&amp;&amp;dist[i]&lt;MIN)</span><br><span class="line">        &#123;</span><br><span class="line">            min_node=i;</span><br><span class="line">            MIN=dist[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">//若找不到小于INFINITY的dist[min_node],则剩下节点与起点start不连通</span></span><br><span class="line">    </span><br><span class="line">    visited[u]=<span class="literal">true</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[u].<span class="built_in">size</span>();++i)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> next=Edge[u][i].end;	<span class="comment">//u能访问到的节点</span></span><br><span class="line">        <span class="keyword">if</span>(visited[next]==<span class="literal">false</span>&amp;&amp;dist[next]&gt;dist[u]+Edge[u][i].value)</span><br><span class="line">            dist[next]=dist[u]+Edge[u][i].value;	<span class="comment">//松弛操作</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h6 id="优先队列实现">2.优先队列实现</h6>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> pair&lt;<span class="keyword">int</span>,<span class="keyword">int</span>&gt; node;		<span class="comment">//first为最短距离，second为节点编号</span></span><br><span class="line">priority_queue&lt;node,vector&lt;node&gt;,greater&lt;node&gt;&gt; q;</span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Dijkstra</span><span class="params">(<span class="keyword">int</span> start)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dist[start]=<span class="number">0</span>;</span><br><span class="line">    q.<span class="built_in">push</span>(<span class="built_in">node</span>(<span class="number">0</span>,s));			<span class="comment">//将起点推入队列中</span></span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>())</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> u=q.<span class="built_in">top</span>().second;</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        <span class="keyword">int</span> now=u.second;		<span class="comment">//顶点编号</span></span><br><span class="line">        <span class="keyword">if</span>(visited[now]==<span class="literal">true</span>)</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[now].<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> next=Edge[now][i].end;</span><br><span class="line">            <span class="keyword">if</span>(dist[next]&gt;dist[now]+Edge[now][i].value)</span><br><span class="line">            &#123;</span><br><span class="line">                dist[next]=dist[now]+Edge[now][i].value;</span><br><span class="line">                q.<span class="built_in">push</span>(<span class="built_in">node</span>(dist[next],next));</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h4 id="bellman-ford算法">Bellman-Ford算法</h4>
<p>如果图具有负的边权，Dijkstra算法行不通。问题在于，一旦一个顶点<span
class="math inline">\(u\)</span>被声明为<em>known</em>的，那就可能存在从某个另外的<em>unknown</em>顶点<span
class="math inline">\(v\)</span>有一条回到<span
class="math inline">\(u\)</span>的边权负值非常大的路径。在这种情形下，选取从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(v\)</span>再回到<span
class="math inline">\(u\)</span>的路径比从<span
class="math inline">\(s\)</span>到<span
class="math inline">\(u\)</span>但不过<span
class="math inline">\(v\)</span>的方法更好。</p>
<p><strong>原理</strong>:<em>Bellman-Ford</em>算法是对图进行<span
class="math inline">\(|V|-1\)</span>次松弛操作，得到所有可能最短路径。每进行一轮循环，就对图上所有的边尝试进行一次松弛操作，当一次循环中没有成功的松弛操作时，算法停止。</p>
<p><strong>优点</strong>:边权可以为负值，实现简单</p>
<p><strong>缺点</strong>:时间复杂度高，为<span
class="math inline">\(O(|V||E|)\)</span></p>
<p><strong>负权环判定</strong>:由于负权环可以无限制降低总花费，所以发现第<span
class="math inline">\(n\)</span>次操作仍可降低花销，则一定存在负权环</p>
<hr />
<p><strong><em>P.S</em></strong> 以<span
class="math inline">\(S\)</span>点为起点执行<em>Bellman-Ford</em>算法时，如果没有给出存在负环的结果，只能说明从<span
class="math inline">\(S\)</span>点出发不能抵达一个负环，不能说明图上不存在负环</p>
<hr />
<h5 id="代码实现">代码实现</h5>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BellmanFord</span><span class="params">(<span class="keyword">int</span> start,<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    dist[start]=<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">bool</span> update;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;n;i++)</span><br><span class="line">    &#123;</span><br><span class="line">        update=<span class="literal">false</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">0</span>;j&lt;n;j++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">0</span>;k&lt;Edge[j].<span class="built_in">size</span>();k++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">int</span> next=Edge[j][k].end;</span><br><span class="line">                <span class="keyword">if</span>(dist[next]&gt;dist[next]+Edge[j][k].value)</span><br><span class="line">                &#123;</span><br><span class="line">                    dist[next]=dist[j]+Edge[j][k].value;</span><br><span class="line">                    update=<span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span>(!update)			<span class="comment">//没有可以松弛的边则停止算法</span></span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> update;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h5 id="队列优化">队列优化</h5>
<p><a
href="如何看待%20SPFA%20算法已死这种说法？%20-%20知乎用户的回答%20-%20知乎%20https://www.zhihu.com/question/292283275/answer/484871888">队列优化相关局限性</a></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">BellmanFord_Q</span><span class="params">(<span class="keyword">int</span> start,<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    queue&lt;<span class="keyword">int</span>&gt; q;</span><br><span class="line">    dist[start]=<span class="number">0</span>;</span><br><span class="line">    visited[start]=<span class="literal">true</span>;</span><br><span class="line">    q.<span class="built_in">push</span>(start);</span><br><span class="line">    <span class="keyword">while</span>(!q.<span class="built_in">empty</span>());</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> now=q.<span class="built_in">front</span>();</span><br><span class="line">        q.<span class="built_in">pop</span>();</span><br><span class="line">        visited[now]=<span class="literal">false</span>;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;Edge[now].<span class="built_in">size</span>();i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">int</span> next=Edge[now][i].end;</span><br><span class="line">            <span class="keyword">if</span>(dist[next]&gt;dist[now]+Edge[now][i].value)</span><br><span class="line">            &#123;</span><br><span class="line">                dist[next]=dist[now]+Edge[now][i].value;</span><br><span class="line">                cnt[next]=cnt[now]+<span class="number">1</span>;		<span class="comment">//最短路径经过的边数</span></span><br><span class="line">                <span class="keyword">if</span>(cnt[next]&gt;=n)</span><br><span class="line">                    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">                <span class="comment">//若不经过负环，则最短路至多经过n-1条边</span></span><br><span class="line">                <span class="comment">//若经过大于等于n条边，一定经过负环</span></span><br><span class="line">                <span class="keyword">if</span>(!visited[next])</span><br><span class="line">                &#123;</span><br><span class="line">                    q.<span class="built_in">push</span>(next);</span><br><span class="line">                    visited[next]=<span class="literal">true</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h2 id="任意两点最短路问题">任意两点最短路问题</h2>
<h3 id="floyd-warshall算法">Floyd-Warshall算法</h3>
<p><strong>原理</strong>:设<span
class="math inline">\(D_{k,i,j}\)</span>为从<span
class="math inline">\(v_i\)</span>到<span
class="math inline">\(v_j\)</span>只使用<span
class="math inline">\(v_1,v_2,...v_k\)</span>作为中间节点的最短路径的权。因此,<span
class="math inline">\(D_{0,i,j}=c_{i,j}\)</span>,若</p>
<p>(<span class="math inline">\(v_i,v_j\)</span>)不是该图的边则<span
class="math inline">\(c_{i,j}\)</span>为<span
class="math inline">\(\infin\)</span>。另外，<span
class="math inline">\(D_{|V|,i,j}\)</span>为图中从<span
class="math inline">\(v_i\)</span>到<span
class="math inline">\(v_j\)</span>的最短路径。</p>
<ol type="1">
<li><p>若最短路径经过点<span class="math inline">\(v_k\)</span>，则<span
class="math inline">\(D_{k,i,j}=D_{k-1,k,j}+D_{k-1,i,k}\)</span></p></li>
<li><p>若最短路径不经过点<span
class="math inline">\(v_k\)</span>，则<span
class="math inline">\(D_{k,i,j}=D_{k-1,i,j}\)</span></p></li>
</ol>
<p>则<span
class="math inline">\(D_{k,i,j}=min(D_{k-1,i,j},D_{k-1,k,j}+D_{k-1,i,k})\)</span></p>
<p><strong>时间复杂度</strong>:<span
class="math inline">\(O(N^3)\)</span>​</p>
<hr />
<h4 id="代码实现-1">代码实现</h4>
<p>只使用节点<span class="math inline">\(0\)</span>~<span
class="math inline">\(k\)</span>和<span
class="math inline">\(i,j\)</span>的情况下，记<span
class="math inline">\(i\)</span>到<span
class="math inline">\(j\)</span>的最短路长度为<code>dist[k+1][i][j]</code>。当<span
class="math inline">\(k=-1\)</span>时，认为只使用<span
class="math inline">\(i\)</span>和<span
class="math inline">\(j\)</span>,则<code>dist[0][i][j]=value(i,j)</code></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//数组若声明为局部，则传入参数;全局无需传入参数</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Floyd</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">0</span>;k&lt;node_num;k++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;node_num;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">0</span>;j&lt;node_num;j++)</span><br><span class="line">                dist[k][i][j]=<span class="built_in">min</span>(dist[k<span class="number">-1</span>][i][j],dist[k<span class="number">-1</span>][i][k]+dist[k<span class="number">-1</span>][k][j])</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>第一维对算法结果并无影响，可以省略,因此算法可以修改为</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><span class="line"><span class="comment">//dist[u][v]表示边(u,v)的权值，不存在时设为INF,但dist[i][i]=0</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">Floyd</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span>(<span class="keyword">int</span> k=<span class="number">0</span>;k&lt;node_num;k++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">for</span>(<span class="keyword">int</span> i=<span class="number">0</span>;i&lt;node_num;i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span>(<span class="keyword">int</span> j=<span class="number">0</span>;j&lt;node_num;j++)</span><br><span class="line">                dist[i][j]=<span class="built_in">min</span>(dist[i][j],dist[i][k]+d[k][j]);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<hr />
<h1 id="具体题目">具体题目</h1>
<ul>
<li><p><a
href="https://www.luogu.com.cn/problem/P4779">洛谷P4779</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P5651">洛谷P5651</a></p></li>
<li><p><a
href="https://www.luogu.com.cn/problem/P1144">洛谷P1144</a></p></li>
<li><p><a
href="https://www.acwing.com/problem/content/346/">Acwing344</a></p></li>
</ul>
<hr />
<p>Author: AsukaMilet</p>
<p>Date: 2022-2-22</p>
]]></content>
      <categories>
        <category>Algorithms</category>
      </categories>
      <tags>
        <tag>图论</tag>
        <tag>算法</tag>
      </tags>
  </entry>
</search>
